on: push

jobs:
  lint-job:
    runs-on: ubuntu-latest
    name: Lint
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v3
      - name: Install modules
        run: yarn
      - name: Run ESLint
        run: yarn lint
