// match OL sizes so that covers from them won't get scaled up
export const maxBookCoverSizes = {
  thumbnail: {
    width: 180,
    height: 180,
  },
  full: {
    width: 500,
    height: 500,
  },
}
